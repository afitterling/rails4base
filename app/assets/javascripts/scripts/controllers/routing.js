'use strict';

angular.module('ctrls.routing', [])

  .controller('RoutingCtrl',
    ['$scope', '$rootScope', '$resource', '$http',
      function ($scope, $rootScope, $resource, $http) {

//        console.log('x');

//        $scope.$on('event:loginRequired', function () {
//          $location.path('/login');
//        });

      }]);
